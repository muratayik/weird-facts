import { makeStyles } from "@material-ui/core/styles";


export const useStyles = makeStyles(() => ({
    spinner: {
        marginLeft: 'auto',
        marginRight: 'auto',
        marginTop: '50px'
    },
}));