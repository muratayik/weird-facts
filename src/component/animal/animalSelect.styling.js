import { makeStyles } from "@material-ui/core/styles";


export const useStyles = makeStyles(() => ({
    animalTypeSelectContainer: {
        marginLeft: 'auto',
        marginRight: 'auto',
        paddingTop: '30px'
    },
    animalTypeSelect: {
        minWidth: '300px',
    }
}));